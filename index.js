const custom = document.getElementById("custom");
const menuCustom = document.getElementById("menu-custom");

custom.addEventListener('click', function() {
    menuCustom.classList.toggle("visibility");
});
